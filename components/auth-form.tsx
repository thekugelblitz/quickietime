"use client";
import {useState} from 'react';
import Link from 'next/link';
import {api} from '@/lib/client';
export function AuthForm(){const [email,setEmail]=useState(''),[code,setCode]=useState(''),[sent,setSent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(){setBusy(true);setError('');try{if(sent){const result=await api<{redirect:string}>('auth/verify',{email,code,returnTo:new URLSearchParams(location.search).get('return_to')||'/?resume=1'});location.assign(result.redirect)}else{await api('auth/request',{email});setSent(true)}}catch(e){setError((e as Error).message)}finally{setBusy(false)}}return <form className="auth-form" onSubmit={e=>{e.preventDefault();void submit()}}><label>Email address<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required disabled={sent||busy}/></label>{sent&&<><p>A six-digit code has been sent to your email. It expires in 10 minutes.</p><label>Verification code<input inputMode="numeric" autoComplete="one-time-code" pattern="[0-9]{6}" maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,''))} required autoFocus/></label></>}{error&&<p role="alert" className="error">{error}</p>}<button className="primary-button" disabled={busy}>{busy?'One moment…':sent?'Verify & sign in':'Email me a code'}</button>{sent&&<button type="button" onClick={()=>{setSent(false);setCode('');setError('')}}>Change email or request another code</button>}<p className="setting-hint">New here? Verification creates your free account. By continuing, you agree to our <Link href="/terms">Terms</Link> and acknowledge our <Link href="/privacy">Privacy policy</Link>.</p></form>}
